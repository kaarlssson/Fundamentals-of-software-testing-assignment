
#******************** Settings ************************

*** Settings ***

# TODO: Write Documentation e.g. User Registration Test scenario with verification, valid and invalid credentials.

Documentation
	

# TODO: Write Precondition, Postcondition, Tags

Pre Condition	Ensure the user has internet connectivity. The user must be logged out of GitLab.
Post Condition	User is registered or an error message is displayed. Browser is closed.
Tags		Registration, UserManagement, Positive, Negative

#******************* Common Variables ********************

*** Variables ***

# NOTE: Please Remove your login credential before uploading Assignments

# TODO: Write Registration field variables

LoginUrl = https://gitlab.com/users/sign_up
VALID_FULL_NAME
VALID_USERNAME
VALID_EMAIL
VALID_PASSWORD
INVALID_EMAIL_FORMAT
EMPTY =

#******************** Test Cases *************************

*** Test Case ***

# TODO: Write Positive and Negative Test Cases and Test Process steps (Think about possible test cases)

# Positive Test examples are below write test process steps for each below test case

	Register user with valid username and password 
	- Remember credentials which you are going to use for whole application testing 

[Documentation]    Tests successful registration using all required and valid credentials.
[Tags]             Positive, Success

# 1. Open the registration page
    Go To    ${LoginUrl}

# 2. Fill in full name
    Input Text    id:new_user_name    ${VALID_FULL_NAME}

# 3. Fill in username
    Input Text    id:new_user_username    ${VALID_USERNAME}

# 4. Fill in email
    Input Text    id:new_user_email    ${VALID_EMAIL}

# 5. Fill in password
    Input Password    id:new_user_password    ${VALID_PASSWORD}

# 6. Click the register button
    Click Button    id:register-button

# 7. Verify success (Expected Post Condition: User lands on profile/onboarding page)
    Wait Until Page Contains    Welcome to GitLab!    
    Page Should Contain    Welcome to GitLab!

# Negative Test examples keywords are below write test process steps for each below test case 

Register user with same above username
    [Documentation]    Verify registration fails when using an already taken username.
    [Tags]             Negative, DuplicateData

Go To    ${LoginUrl}
    Input Text    id:new_user_name    ${VALID_FULL_NAME}
    Input Text    id:new_user_username    ${VALID_USERNAME}
    Input Text    id:new_user_email    example@example.com
    Input Password    id:new_user_password    ${VALID_PASSWORD}
    Click Button    id:register-button

# Verify error
	Wait Until Page Contains Element
	Page Should Contain    Username is already taken

Register user with empty password
    [Documentation]    Verify registration fails with an empty password field.
    [Tags]             Negative, EmptyField

Go To    ${LoginUrl}
    Input Text    id:new_user_name    ${VALID_FULL_NAME}
    Input Text    id:new_user_username    ${RANDOM}
    Input Text    id:new_user_email    another.email@example.com
    Input Password    id:new_user_password    ${EMPTY}
    Click Button    id:register-button

# Verify error
	Wait Until Page Contains Element
	Page Should Contain	Instructions for passwords

Register user with invalid email
    [Documentation]    Verify registration fails with an incorrectly formatted email address.
    [Tags]             Negative, InvalidInput

Go To    ${LoginUrl}
    Input Text    id:new_user_name    ${VALID_FULL_NAME}
    Input Text    id:new_user_username    third_test_${RANDOM}
    Input Text    id:new_user_email    ${INVALID_EMAIL_FORMAT}
    Input Password    id:new_user_password    ${VALID_PASSWORD}
    Click Button    id:register-button

# Verify error
	Wait Until Page Contains Element
	Page Should Contain	Email address without top-level domain. Make sure that you have entered the correct email address. 






